import Layout from "../components/template/Layout";

export default function Settings(props) {
    return(
        <Layout title='Settings' subtitle='System settings'>
            <h1>Settings.....</h1>
        </Layout>
    )
}